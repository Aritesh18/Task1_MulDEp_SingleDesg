import { DepartmentEmployees } from './department-employees';

describe('DepartmentEmployees', () => {
  it('should create an instance', () => {
    expect(new DepartmentEmployees()).toBeTruthy();
  });
});
